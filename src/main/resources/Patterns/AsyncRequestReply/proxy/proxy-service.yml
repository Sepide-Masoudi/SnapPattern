apiVersion: v1
kind: Service
metadata:
  name: proxy-service
  namespace: pattern
spec:
  selector:
    app: proxy-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP