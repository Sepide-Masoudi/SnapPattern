apiVersion: v1
kind: Service
metadata:
  labels:
    app: pattern
  name: ${SERVICE_NAME}
  namespace: ${NAMESPACE}
spec:
  selector:
    app: cache-proxy-async
  ports:
    - protocol: TCP
      port: ${SERVICE_PORT}
      targetPort: 80