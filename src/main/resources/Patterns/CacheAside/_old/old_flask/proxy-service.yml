apiVersion: v1
kind: Service
metadata:
  name: proxy-cache
  namespace: pattern
  labels:
    pattern-managed: "true"
    pattern-type: "cache-aside"
spec:
  selector:
    app: teastore-persistence-proxy-cache
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
