kubectl get svc teastore-persistence -n user -o yaml > svc-backend.yaml

kubectl delete svc teastore-persistence -n user

rename svc-backup.yaml metadata:name --> {$SERVICE_NAME}-backend

kubectl apply -f svc-backend.yaml

docker build -t custom-flask-cache-proxy .

minikube image load custom-flask-cache-proxy

kubectl apply -f proxy-service.yml

kubectl apply -f proxy-deployment.yml

helm repo add bitnami https://charts.bitnami.com/bitnami
helm repo update
helm upgrade -install redis-cache bitnami/redis-cluster -n pattern --create-namespace --set usePassword=false --set replica.replicaCount=2 --set cluster.nodes=6



Istio:

curl -L https://istio.io/downloadIstio | sh -
cd istio-*/bin

export PATH=$PWD:$PATH

istioctl install --set profile=default --set values.global.platform=minikube --skip-confirmation

kubectl label namespace user istio-injection=enabled --overwrite

kubectl apply -f cache-routing.yml