apiVersion: v1
kind: Service
metadata:
  name: ${BACKEND_SERVICE}
  namespace: default
spec:
  selector:
    app: ${BACKEND_SERVICE}-cache-proxy
  ports:
    - protocol: TCP
      port: ${BACKEND_PORT}
      targetPort: 80
